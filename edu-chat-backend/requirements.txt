# edu-chat-backend 依赖（意图网关仓库 agent-intent-classifier 需另行可用，
# 默认取兄弟目录 ../yitubest，可用环境变量 INTENT_GATEWAY_PATH 指定）
fastapi>=0.110
uvicorn[standard]>=0.29
langgraph>=0.2
langchain-core>=0.1
requests>=2.31
pydantic>=2.0
